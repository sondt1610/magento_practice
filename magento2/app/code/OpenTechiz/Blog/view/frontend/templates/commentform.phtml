<script type="text/x-magento-init">
    {
        "*": {
            "OpenTechiz_Blog/js/commentajax": {
                "AjaxCommentPostUrl": "<?php echo $block->getFormAction() ?>",
                "PostId": "<?php echo $block->getPostId() ?>",
                "LoadUrl": "<?php echo $block->getLoadUrl() ?>",
                "ActiveUrl": "<?php echo $block->getActiveUrl() ?>"
            }
        }
    }
</script>

<form class="form" method="post" id="comment_ajax">

    <fieldset class="fieldset">
        <legend class="legend"><span><?php /* @escapeNotVerified */ echo __('Add new comment') ?></span></legend><br>
        <div class="field required">
            <div class="note"></div>
            <label for="author" class="label">
                <span><?php /* @escapeNotVerified */ echo __('Name') ?></span>
            </label>

            <div class="control">
                <input type="text" id="author"
                       name="author"
                       class="input-text" width="50%">
            </div>
        </div>
        <div class="field required">
            <label for="content" class="label">
                <span><?php /* @escapeNotVerified */ echo __('Content') ?></span>
            </label>

            <div class="control">
                <textarea id="content" name="content" class="input-text"></textarea>
            </div>
        </div>
        <div class="field email required">
            <label class="label" for="email"><span><?= $block->escapeHtml(__('Email')) ?></span></label>
            <div class="control">
                <input name="email" id="email" title="<?= $block->escapeHtmlAttr(__('Email')) ?>" value="<?= $block->escapeHtmlAttr($this->helper('Magento\Contact\Helper\Data')->getPostValue('email') ?: $this->helper('Magento\Contact\Helper\Data')->getUserEmail()) ?>" class="input-text" type="email" data-validate="{required:true, 'validate-email':true}"/>
            </div>
        </div>
    </fieldset>
    <input type="hidden" name="post_id" value="<?php echo $block->getPostID() ?>"/>
    <div class="actions-toolbar">
        <div class="primary">
            <button type="submit" class="action submit primary" title="<?php /* @escapeNotVerified */ echo __('Submit') ?>">
                <span><?php /* @escapeNotVerified */ echo __('Submit') ?></span>
            </button>
        </div>
    </div>

</form>

